# PostgreSQL password shared by all database services.
# Copy this file to .env and set a strong password before running docker compose.
POSTGRES_PASSWORD=change_me

# Secret key for JWT signing and verification shared across microservices
JWT_SECRET=change_me_jwt_secret_key

